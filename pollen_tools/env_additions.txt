# ── Add these lines to the Pollen app's .env on the robot ───────────────────
# Copy from /home/user/reachy-gpt-app/.env:
TAVILY_API_KEY=<paste your key here>

# Tell the app where to find the web_search.py tool:
REACHY_MINI_EXTERNAL_TOOLS_DIRECTORY=external_content/external_tools

# Auto-discover all .py files in that directory (no need to edit tools.txt):
AUTOLOAD_EXTERNAL_TOOLS=1
